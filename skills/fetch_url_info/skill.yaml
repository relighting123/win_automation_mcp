description: "OpenChrome으로 URL을 열고 본문 텍스트를 가져옵니다 (사내 SSO)"
tools:
  - tool: openchrome/navigate
    args:
      url: { mode: "ai" }
  - tool: openchrome/read_page
    args:
      mode: { mode: "fixed", value: "markdown" }
      onlyMainContent: { mode: "fixed", value: true }
