description: "FileSystem MCP 도구로 파일 내용을 읽고 특정 소스 구간을 수정"
tools:
  - tool: read_text_file
    args:
      path: { mode: "ai" }
  - tool: edit_file
    args:
      path: { mode: "ai" }
      edits: { mode: "ai" }
      dryRun: false
  - tool: read_text_file
    args:
      path: { mode: "ai" }
